# Backend — MySQL local only. Do NOT set DB_URL to PostgreSQL in IDE Run Configuration.
# If IntelliJ/VS Code still has env DB_URL=jdbc:postgresql... remove it or it may confuse you.

# Optional overrides (only if you know what you are doing):
# SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/warmup_library?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# SPRING_DATASOURCE_USERNAME=root
# SPRING_DATASOURCE_PASSWORD=2004

PORT=8080
CORS_ORIGINS=http://localhost:5173
