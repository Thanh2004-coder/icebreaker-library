# Local `npm run dev` can leave this empty (Vite proxies /api → http://localhost:8080).
# Production (Vercel Hobby): set this in the Vercel project settings BEFORE building.
# Example: https://warmup-api.onrender.com
VITE_API_URL=

